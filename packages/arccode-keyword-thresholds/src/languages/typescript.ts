import { typescript } from 'language-keywords'

import createBaseThresholds from '../createBaseThresholds'

const typescriptThresholds = createBaseThresholds(typescript, {
})

export default typescriptThresholds
